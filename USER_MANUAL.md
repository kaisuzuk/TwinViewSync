# TwinView Sync 利用者向け説明書

TwinView Sync は、Vue2 画面と Vue3 画面など、2つのブラウザタブを並べて見た目や操作結果を確認するための Chrome / Edge 拡張機能です。

操作同期、スクリーンショット重ね合わせ、点滅比較、差分ハイライト、ピクセルグリッドを使って、移行後画面のレイアウト差分を効率よく確認できます。

---

## できること

| 機能                   | 用途                                                                           |
| ---------------------- | ------------------------------------------------------------------------------ |
| タブペアリング         | 比較したい2つのタブを Tab A / Tab B として登録する                             |
| 操作同期               | マウス移動、クリック、ドラッグ、スクロール、入力などをもう一方のタブへ反映する |
| ゴーストカーソル       | 相手タブ上でのカーソル位置を表示する                                           |
| クリック位置ハイライト | クリック位置を波紋アニメーションで表示する                                     |
| Compare Overlay        | 片方の画面キャプチャをもう一方へ半透明で重ねる                                 |
| Blink Compare          | 実画面とキャプチャ画像を交互に表示する                                         |
| Diff Highlight         | 2画面のピクセル差分を赤色マスクで表示する                                      |
| Grid Overlay           | ピクセルグリッドを表示して余白や位置を確認する                                 |

---

## 利用前の準備

### 対応ブラウザ

- Google Chrome
- Microsoft Edge

### 拡張機能の読み込み

配布された `extension/dist/` フォルダをブラウザに読み込みます。

Chrome の場合:

1. Chrome で `chrome://extensions/` を開く
2. 右上の **デベロッパーモード** を ON にする
3. **パッケージ化されていない拡張機能を読み込む** をクリック
4. `extension/dist/` フォルダを選択する

Edge の場合:

1. Edge で `edge://extensions/` を開く
2. **デベロッパーモード** を ON にする
3. **展開して読み込む** をクリック
4. `extension/dist/` フォルダを選択する

---

## 基本的な使い方

### 1. 比較する2画面を開く

比較したい2つの画面を、それぞれ別タブで開きます。

例:

- Tab A: Vue2 画面
- Tab B: Vue3 画面

### 2. Tab A を登録する

1. Vue2 画面など、基準にしたいタブを表示する
2. ブラウザ右上の TwinView Sync アイコンをクリックする
3. **Tab A** 行の **Set Current** をクリックする

### 3. Tab B を登録する

1. 比較先のタブを表示する
2. TwinView Sync のポップアップを開く
3. **Tab B** 行の **Set Current** をクリックする

Tab A と Tab B には、同じタブを登録できません。

### 4. 同期方向を選ぶ

**Sync Control** の **Direction** で同期方向を選びます。

| 選択肢        | 動作                                 |
| ------------- | ------------------------------------ |
| A → B         | Tab A の操作を Tab B に反映する      |
| B → A         | Tab B の操作を Tab A に反映する      |
| Bidirectional | どちらのタブの操作も相手側に反映する |

### 5. 操作同期を開始する

**Enable Sync** をクリックすると同期が開始されます。

ポップアップ右上のバッジが **ON** になっていれば同期中です。

同期を一時停止したい場合は **Pause** をクリックします。再開する場合は **Resume** をクリックします。

---

## 操作同期で反映される内容

以下の操作が相手タブに反映されます。

- マウス移動
- クリック
- ダブルクリック
- 右クリック
- ドラッグ操作
- マウスホイール
- ページスクロール
- ダイアログやテーブル内のスクロール
- テキスト入力
- セレクトボックスやチェックボックスの変更
- キーボード操作
- SPA 画面の URL / ルート変更

クリックや入力も同期されるため、登録・削除・送信などの副作用がある操作には注意してください。

---

## Compare Overlay

Compare Overlay は、片方の画面キャプチャをもう一方の画面へ半透明で重ねる機能です。

Vue2 画面を正解として Vue3 画面に重ねることで、余白、位置、サイズ、文字折り返しなどの差分を目視しやすくなります。

### 手順

1. **Compare Overlay** セクションで方向を選ぶ
2. **Opacity** で重ねる画像の透明度を調整する
3. **Capture Reference** をクリックする
4. 対象タブにキャプチャ画像が重なって表示される
5. **Show Overlay / Hide Overlay** で表示を切り替える

例:

| 方向  | 動作                                 |
| ----- | ------------------------------------ |
| A → B | Tab A をキャプチャし、Tab B に重ねる |
| B → A | Tab B をキャプチャし、Tab A に重ねる |

スクロール、リサイズ、画面状態の変更後は、再度 **Capture Reference** を実行してください。

URLが変わった場合、Overlay は自動でクリアされます。

---

## Blink Compare

Blink Compare は、実画面とキャプチャ画像を交互に表示する機能です。

重ね合わせだけでは分かりにくいズレや表示差分を確認しやすくなります。

### 手順

1. 先に Compare Overlay の **Capture Reference** を実行する
2. **Blink Compare** セクションで **Interval (ms)** を設定する
3. **Start Blink** をクリックする
4. 停止したい場合は **Stop Blink** をクリックする

初期値は 500ms です。点滅が速すぎる場合は数値を大きくしてください。

---

## Diff Highlight

Diff Highlight は、Tab A と Tab B の表示領域をそれぞれキャプチャし、ピクセル差分がある箇所を赤色マスクで表示する機能です。

細かな色差、余白差、位置ズレを機械的に見つけたい場合に使います。

### 手順

1. **Diff Highlight** セクションで方向を選ぶ
2. **Opacity** で赤色マスクの濃さを調整する
3. **Threshold** で差分検出の感度を調整する
4. **Capture Diff** をクリックする
5. 対象タブに差分箇所が赤く表示される
6. **Show Diff / Hide Diff** で表示を切り替える

### Threshold の目安

| 値       | 特徴                                         |
| -------- | -------------------------------------------- |
| 小さい値 | 小さな色差も検出する。差分が多く出やすい     |
| 大きい値 | 大きな差分だけ検出する。ノイズを減らしやすい |

初期値は 32 です。差分が出すぎる場合は値を上げ、差分が出にくい場合は値を下げてください。

スクロール、リサイズ、画面状態の変更後は、再度 **Capture Diff** を実行してください。

URLが変わった場合、Diff Highlight は自動でクリアされます。

---

## Grid Overlay

Grid Overlay は、画面上にピクセルグリッドを表示する機能です。

要素の位置、余白、整列、幅のズレを確認したい場合に使います。

### 設定項目

| 項目     | 内容                                                    |
| -------- | ------------------------------------------------------- |
| Size     | グリッドの間隔。8 / 10 / 12 / 16px または Custom を選択 |
| Opacity  | グリッドの透明度                                        |
| Offset X | 横方向の開始位置                                        |
| Offset Y | 縦方向の開始位置                                        |

**Show Grid** で表示し、**Hide Grid** で非表示にします。

---

## URL変更時の自動クリア

Compare Overlay、Blink Compare、Diff Highlight は、画面キャプチャを元に表示します。

そのため、どちらかのタブで URL が変わった場合は、古い画面のキャプチャが残らないように自動でクリアされます。

URL変更後に再度比較したい場合は、対象画面が表示された状態で次の操作をやり直してください。

- Compare Overlay: **Capture Reference**
- Diff Highlight: **Capture Diff**
- Blink Compare: Compare Overlay のキャプチャ後に **Start Blink**

---

## 安全に使うための注意

- 操作同期中は、クリックや入力も相手タブへ反映されます
- 登録、削除、送信、確定などの操作は、意図せず相手画面でも実行される可能性があります
- 副作用がある画面では、同期を OFF にするか、検証用データで操作してください
- パスワード入力やファイル選択など、ブラウザの制限により同期できない操作があります
- iframe 内部の操作は対象外です
- ビューポートサイズが大きく異なる場合、操作位置やスクロール位置が完全には一致しないことがあります

---

## うまく動かない場合

### 同期されない

- Tab A と Tab B の両方が登録されているか確認する
- 同じタブを Tab A / Tab B に登録していないか確認する
- 右上のバッジが **ON** になっているか確認する
- **Pause** 状態になっていないか確認する
- 同期方向が意図した向きになっているか確認する
- 対象ページを再読み込みしてから再度試す

### Overlay / Diff が古い画面のままに見える

- **Capture Reference** または **Capture Diff** を再実行する
- スクロールや画面状態を変えた後は再キャプチャする
- URL変更後は自動クリアされるため、再度キャプチャする

### 差分が多すぎる

- Diff Highlight の **Threshold** を上げる
- アニメーション、時刻表示、ランダム表示、カーソル表示などが差分になっていないか確認する
- 2つのタブのスクロール位置や表示状態を揃えてから再キャプチャする

### 操作位置がずれる

- 2つのタブのウィンドウサイズを近づける
- 同じスクロール位置に揃える
- レイアウト差が大きい場合は、操作同期より Overlay / Diff / Grid を使って確認する

---

## 推奨ワークフロー

1. Tab A に基準画面、Tab B に比較画面を登録する
2. Sync を ON にして、基本操作と画面遷移を確認する
3. Grid Overlay で余白や整列を確認する
4. Compare Overlay で基準画面を重ねて確認する
5. Blink Compare でズレを目視確認する
6. Diff Highlight で細かなピクセル差分を確認する
7. 画面状態やURLが変わったら、必要に応じて再キャプチャする
